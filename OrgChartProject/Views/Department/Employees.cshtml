@model IEnumerable<OrgChartProject.Models.Employee>

<h2>Employees</h2>

<ul>
    @foreach (var employee in Model)
    {
        <li>@employee.FirstName @employee.LastName - @employee.Position</li>
    }
</ul>